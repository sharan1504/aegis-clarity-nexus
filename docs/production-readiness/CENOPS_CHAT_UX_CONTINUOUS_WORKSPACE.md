# CenOps Chat UX — Continuous Workspace

The Copilot conversation uses a single continuous workspace rather than an outer Card containing a second chat surface. The page header, workspace context, conversation area, and composer are separated by subtle rules only; response and recommendation cards remain available inside the conversation where they add meaning.