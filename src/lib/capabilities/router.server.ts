// Capability Router — the single enforcement point between agents and providers.
// Agents never name a provider, never see credentials, and can only reach
// integrations + capabilities the tenant explicitly bound to them. Server-only.
import type { SupabaseClient } from "@supabase/supabase-js";
import type { Database } from "@/integrations/supabase/types";
import { evaluateGuardrails } from "@/lib/guardrails/engine.server";
import type { GuardrailVerdict } from "@/lib/guardrails/evaluate";
import { sanitizeOutput } from "@/lib/guardrails/sanitize";
import { Microsoft365LicenseConnector, type Microsoft365Connection } from "@/lib/microsoft365/connector.server";
import { readConnectionCredentials } from "@/lib/integrations/oauth-framework.server";
import { authorizeCapabilityAccess, DENIAL_MESSAGES, type AuthorizedSource, type AuthorizationDecision } from "./authorization.server";
import { evaluateFreshness, worstFreshness } from "./freshness";
import type { AgentPolicy, PolicyRevision } from "./policy";
import type { CapabilityKey, CapabilityResult, CapabilitySource, NormalizedEntitlement, NormalizedQueue, NormalizedUser, RecordProvenance } from "./registry";

type UserClient = SupabaseClient<Database>;
export type { AuthorizedSource } from "./authorization.server";

async function privilegedDb() { const { supabaseAdmin } = await import("@/integrations/supabase/client.server"); return supabaseAdmin; }

function provenanceFor(source: AuthorizedSource, sourceSystem: string, storeName: string, row: { snapshot_id?: string | null; sync_id?: string | null; synced_at?: string | null }): RecordProvenance { return { provider: source.provider, integrationId: source.integrationId, sourceSystem, source: storeName, snapshotId: row.snapshot_id ?? source.snapshotId, syncId: row.sync_id ?? source.syncRunId, dataAsOf: row.synced_at ?? source.lastSyncAt, lastSuccessfulSyncAt: source.lastSyncAt, freshness: source.freshness.state }; }
interface ProviderImpl { sourceSystem: string; license_inventory?: (s: AuthorizedSource, tenantId: string) => Promise<NormalizedEntitlement[]>; user_inventory?: (s: AuthorizedSource, tenantId: string) => Promise<NormalizedUser[]>; queue_inventory?: (s: AuthorizedSource, tenantId: string) => Promise<NormalizedQueue[]>; }

const CANONICAL_PROVIDER_IDS: Record<string, string> = { m365: "microsoft365", microsoft365: "microsoft365" };
export function canonicalProviderId(provider: string): string { const normalized = provider.trim().toLowerCase(); return CANONICAL_PROVIDER_IDS[normalized] ?? normalized; }

const genesysImpl: ProviderImpl = {
  sourceSystem: "Genesys Cloud",
  async license_inventory(source, tenantId) { const db = await privilegedDb(); const [{ data: assignments }, { data: licenses }, { data: users }] = await Promise.all([db.from("genesys_user_licenses").select("genesys_user_id, license_id, snapshot_id, sync_id, synced_at").eq("tenant_id", tenantId).eq("integration_id", source.integrationId).eq("is_current", true), db.from("genesys_licenses").select("license_id, name").eq("tenant_id", tenantId).eq("integration_id", source.integrationId).eq("is_current", true), db.from("genesys_users").select("genesys_user_id, name, email, state, last_login_at, date_created").eq("tenant_id", tenantId).eq("integration_id", source.integrationId).eq("is_current", true)]); const licenseName = new Map((licenses ?? []).map((l) => [l.license_id, l.name])); const userById = new Map((users ?? []).map((u) => [u.genesys_user_id, u])); return (assignments ?? []).map((a) => { const user = userById.get(a.genesys_user_id); return { provider: "genesys", integrationId: source.integrationId, userId: a.genesys_user_id, userName: user?.name ?? null, userEmail: user?.email ?? null, entitlementId: a.license_id, entitlementName: licenseName.get(a.license_id) ?? a.license_id, status: user?.state === "active" ? "active" : user?.state ? "inactive" : "unknown", lastActivityAt: user?.last_login_at ?? null, metadata: { genesysState: user?.state ?? null, accountCreatedAt: user?.date_created ?? null }, provenance: provenanceFor(source, "Genesys Cloud", "genesys_user_licenses", a) } satisfies NormalizedEntitlement; }); },
  async user_inventory(source, tenantId) { const db = await privilegedDb(); const { data } = await db.from("genesys_users").select("genesys_user_id, name, email, state, last_login_at, title, department, division_name, date_created, snapshot_id, sync_id, synced_at").eq("tenant_id", tenantId).eq("integration_id", source.integrationId).eq("is_current", true); return (data ?? []).map((u) => ({ provider: "genesys", integrationId: source.integrationId, userId: u.genesys_user_id, userName: u.name, userEmail: u.email, status: u.state, lastActivityAt: u.last_login_at, metadata: { title: u.title, department: u.department, division: u.division_name, accountCreatedAt: u.date_created }, provenance: provenanceFor(source, "Genesys Cloud", "genesys_users", u) })); },
  async queue_inventory(source, tenantId) { const db = await privilegedDb(); const { data } = await db.from("genesys_queues").select("queue_id, name, member_count, division_name, description, snapshot_id, sync_id, synced_at").eq("tenant_id", tenantId).eq("integration_id", source.integrationId).eq("is_current", true); return (data ?? []).map((q) => ({ provider: "genesys", integrationId: source.integrationId, queueId: q.queue_id, queueName: q.name, memberCount: q.member_count, metadata: { division: q.division_name, description: q.description }, provenance: provenanceFor(source, "Genesys Cloud", "genesys_queues", q) })); },
};

async function microsoft365Snapshot(source: AuthorizedSource, tenantId: string) { const db = await privilegedDb(); const { data: connection, error } = await db.from("provider_connections").select("encrypted_credentials").eq("tenant_id", tenantId).eq("integration_id", source.integrationId).maybeSingle(); if (error) throw error; if (!connection?.encrypted_credentials) throw new Error("Microsoft 365 credentials are missing for this integration source."); const { decryptCredentials } = await import("@/lib/integrations/credential-vault.server"); const credentials = decryptCredentials<Microsoft365Connection>(connection.encrypted_credentials); return new Microsoft365LicenseConnector(credentials).sync(); }
const microsoft365Impl: ProviderImpl = {
  sourceSystem: "Microsoft Graph",
  async user_inventory(source, tenantId) { const result = await microsoft365Snapshot(source, tenantId); return result.users.map((user) => ({ provider: "microsoft365", integrationId: source.integrationId, userId: user.externalId, userName: user.name, userEmail: user.email, status: user.status, lastActivityAt: null, metadata: user.metadata ?? {}, provenance: { provider: "microsoft365", integrationId: source.integrationId, sourceSystem: "Microsoft Graph", source: "microsoft_graph.users", snapshotId: null, syncId: null, dataAsOf: result.snapshot.syncedAt, lastSuccessfulSyncAt: result.snapshot.lastSuccessfulSyncAt, freshness: result.snapshot.freshness } })); },
  async license_inventory(source, tenantId) { const result = await microsoft365Snapshot(source, tenantId); const users = new Map(result.users.map((u) => [u.externalId, u])); const licenses = new Map(result.licenses.map((l) => [l.externalId, l])); return result.assignments.map((assignment) => { const user = users.get(assignment.userExternalId); const license = licenses.get(assignment.licenseExternalId); return { provider: "microsoft365", integrationId: source.integrationId, userId: assignment.userExternalId, userName: user?.name ?? null, userEmail: user?.email ?? null, entitlementId: assignment.licenseExternalId, entitlementName: license?.name ?? assignment.licenseExternalId, status: assignment.status === "active" ? "active" : assignment.status === "inactive" ? "inactive" : "unknown", lastActivityAt: assignment.lastActivityAt ?? null, metadata: { ...(assignment.metadata ?? {}), licenseStatus: license?.status ?? null }, provenance: { provider: "microsoft365", integrationId: source.integrationId, sourceSystem: "Microsoft Graph", source: "microsoft_graph.assignments", snapshotId: null, syncId: null, dataAsOf: result.snapshot.syncedAt, lastSuccessfulSyncAt: result.snapshot.lastSuccessfulSyncAt, freshness: result.snapshot.freshness } } satisfies NormalizedEntitlement; }); },
};
export const PROVIDERS: Record<string, ProviderImpl> = { genesys: genesysImpl, microsoft365: microsoft365Impl };
export function providerImplements(provider: string, capability: CapabilityKey): boolean { const impl = PROVIDERS[canonicalProviderId(provider)]; return !!impl && typeof (impl as unknown as Record<string, unknown>)[capability] === "function"; }
export interface CapabilityCallOptions { now?: number; }
export interface CapabilityPolicySet { [integrationId: string]: { policy: AgentPolicy; revision: PolicyRevision }; }
export interface RoutedCapabilityResult<T> extends CapabilityResult<T> { denied?: { reason: string; message: string }; policies: CapabilityPolicySet; guardrails: Record<string, GuardrailVerdict>; }
function emptyResult<T>(capability: CapabilityKey, agentKey: string, decision: AuthorizationDecision, now: number): RoutedCapabilityResult<T> { return { capability, tenantId: decision.tenantId ?? "", agentKey, records: [], sources: [], warnings: decision.reason ? [DENIAL_MESSAGES[decision.reason]] : [], evaluatedAt: new Date(now).toISOString(), freshness: "unavailable", policies: {}, guardrails: {}, ...(decision.reason ? { denied: { reason: decision.reason, message: DENIAL_MESSAGES[decision.reason] } } : {}) }; }
async function runCapability<T>(supabase: UserClient, userId: string, agentKey: string, capability: CapabilityKey, pick: (impl: ProviderImpl) => ((s: AuthorizedSource, t: string) => Promise<T[]>) | undefined, options: CapabilityCallOptions = {}): Promise<RoutedCapabilityResult<T>> { const now = options.now ?? Date.now(); const decision = await authorizeCapabilityAccess(supabase, userId, agentKey, capability, { now }); if (!decision.ok || !decision.tenantId) return emptyResult<T>(capability, agentKey, decision, now); const tenantId = decision.tenantId; const result: RoutedCapabilityResult<T> = { capability, tenantId, agentKey, records: [], sources: [], warnings: [], evaluatedAt: new Date(now).toISOString(), freshness: "unavailable", policies: {}, guardrails: {} }; for (const denial of decision.denials) result.warnings.push(denial.reason === "capability_not_supported_by_provider" ? `${denial.provider} does not yet supply ${capability}.` : DENIAL_MESSAGES[denial.reason]); for (const source of decision.sources) { const provider = canonicalProviderId(source.provider); const sourceNormalized = { ...source, provider }; result.policies[source.integrationId] = { policy: source.policy, revision: source.policyRevision }; const freshness = evaluateFreshness(source.lastSyncAt, now); const baseSource: CapabilitySource = { integrationId: source.integrationId, provider, displayName: source.displayName, implemented: true, recordCount: 0, lastSyncAt: source.lastSyncAt, snapshotId: source.snapshotId, freshness: freshness.state, freshnessAgeMs: freshness.ageMs, policyVersion: source.policyRevision.version }; const fn = pick(PROVIDERS[provider] ?? { sourceSystem: provider }); if (!source.implemented || !fn) { const warning = `${source.displayName} does not yet supply ${capability}.`; result.warnings.push(warning); result.sources.push({ ...baseSource, implemented: false, warning }); continue; } const verdict = await evaluateGuardrails(supabase, { tenantId, actorRole: decision.roles[0] ?? null, origin: "capability_router", agentKey, provider, integrationId: source.integrationId, capability, actionKey: `capability.${capability}`, environment: source.isMock ? "development" : "production", executionClass: "read_only", freshness: freshness.state, dataClassification: "confidential" }, { userId }); result.guardrails[source.integrationId] = verdict; if (!verdict.allowed) { const warning = verdict.reasons[0] ?? `A guardrail prevented ${source.displayName} from being read.`; result.warnings.push(warning); result.sources.push({ ...baseSource, warning }); continue; } try { const records = await fn(sourceNormalized, tenantId); const capped = verdict.maxRecords != null ? records.slice(0, verdict.maxRecords) : records; const safe = sanitizeOutput(capped, verdict.redactFields); if (capped.length < records.length) result.warnings.push(`${source.displayName} results were limited to ${verdict.maxRecords} records by a guardrail.`); result.records.push(...safe); result.sources.push({ ...baseSource, recordCount: safe.length }); } catch (error) { console.error("[capability-router] provider read failed", { provider, capability, integrationId: source.integrationId }); void error; const warning = `${source.displayName} could not be read for ${capability}.`; result.warnings.push(warning); result.sources.push({ ...baseSource, warning }); } } result.freshness = worstFreshness(result.sources.map((s) => s.freshness)); return result; }
export const capabilityRouter = { getLicenseInventory: (supabase: UserClient, userId: string, agentKey: string, options?: CapabilityCallOptions) => runCapability<NormalizedEntitlement>(supabase, userId, agentKey, "license_inventory", (impl) => impl.license_inventory, options), getUsers: (supabase: UserClient, userId: string, agentKey: string, options?: CapabilityCallOptions) => runCapability<NormalizedUser>(supabase, userId, agentKey, "user_inventory", (impl) => impl.user_inventory, options), getQueues: (supabase: UserClient, userId: string, agentKey: string, options?: CapabilityCallOptions) => runCapability<NormalizedQueue>(supabase, userId, agentKey, "queue_inventory", (impl) => impl.queue_inventory, options) };
export const CAPABILITY_ROUTER_CALLS: Record<string, (supabase: UserClient, userId: string, agentKey: string, options?: CapabilityCallOptions) => Promise<RoutedCapabilityResult<unknown>>> = { license_inventory: capabilityRouter.getLicenseInventory, user_inventory: capabilityRouter.getUsers, queue_inventory: capabilityRouter.getQueues };
