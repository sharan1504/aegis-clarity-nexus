# Productivity Agent report contract

Copilot report responses should include: provider, user, reporting window, handled count, completed count, open count, throughput per week, average cycle time when calculable, matching work items, source synchronization time, evidence limitations, and confidence. A report action should be offered as a follow-up in Copilot and can open `/productivity` for a detailed drill-down and CSV export.
