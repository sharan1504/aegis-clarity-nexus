import { createServerFn } from "@tanstack/react-start";
import { requireSupabaseAuth } from "@/integrations/supabase/auth-middleware";
import { resolveTenant } from "@/lib/genesys/store.server";
import { resolveDepartmentContext, getDepartmentProviders } from "@/lib/department-access.server";
import { clearEvidenceCache, withEvidenceCache } from "@/lib/evidence-cache.server";
import { recordOperationalIssueSafely } from "@/lib/operational-issues.server";
import { decryptCredentials } from "@/lib/integrations/credential-vault.server";
import { syncGitHub } from "@/lib/integrations/github-connector.server";
import { ensureJiraAccessToken } from "@/lib/integrations/oauth-jira.server";
import { ensureSalesforceAccessToken, withSalesforceAccessToken } from "@/lib/integrations/oauth-salesforce.server";
import { ensureServiceNowAccessToken } from "@/lib/integrations/oauth-servicenow.server";
import { ensureSlackAccessToken } from "@/lib/integrations/oauth-slack.server";

export type Provider = "github" | "slack" | "jira" | "salesforce" | "servicenow";
interface Credentials { accessToken?: string; cloudId?: string; instanceUrl?: string; }
async function json(url: string, token: string, init: RequestInit = {}) { const response = await fetch(url, { ...init, headers: { accept: "application/json", authorization: `Bearer ${token}`, ...(init.headers ?? {}) } }); const text = await response.text(); let body: any = {}; try { body = text ? JSON.parse(text) : {}; } catch { body = { raw: text }; } if (!response.ok) { const error = new Error(`Provider request failed (${response.status}): ${JSON.stringify(body).slice(0, 1000)}`); (error as Error & { status?: number }).status = response.status; throw error; } return body; }
async function fetchProvider(provider: Exclude<Provider, "github">, credentials: Credentials, connectionId: string, tenantId: string) {
  let token = credentials.accessToken;
  if (provider === "jira") token = await ensureJiraAccessToken(connectionId, tenantId);
  if (provider === "salesforce") token = await ensureSalesforceAccessToken(connectionId, tenantId);
  if (provider === "servicenow") token = await ensureServiceNowAccessToken(connectionId, tenantId);
  if (provider === "slack") token = await ensureSlackAccessToken(connectionId, tenantId);
  if (!token) throw new Error(`${provider} has no access token.`);
  if (provider === "slack") { const auth = await json("https://slack.com/api/auth.test", token); if (!auth.ok) throw new Error(String(auth.error ?? "Slack authentication failed.")); const channels = await json("https://slack.com/api/conversations.list?limit=200&exclude_archived=true", token); if (!channels.ok) throw new Error(String(channels.error ?? "Slack channel discovery failed.")); return [{ entityType: "workspace", entityKey: String(auth.team_id), payload: { team: auth.team, teamId: auth.team_id, userId: auth.user_id } }, ...((channels.channels ?? []) as any[]).map((c) => ({ entityType: "channel", entityKey: String(c.id), payload: { name: c.name, isPrivate: c.is_private, memberCount: c.num_members, topic: c.topic?.value ?? "", purpose: c.purpose?.value ?? "" } }))]; }
  if (provider === "salesforce") { const instance = credentials.instanceUrl; if (!instance) throw new Error("Salesforce instance URL is missing from the OAuth token response. Reconnect required."); return withSalesforceAccessToken(connectionId, tenantId, async (salesforceToken) => { const org = await json(`${instance.replace(/\/$/, "")}/services/data/v66.0/connect/organization`, salesforceToken); return [{ entityType: "organization", entityKey: String(org.id ?? tenantId), payload: { organizationName: org.organizationName ?? "Salesforce", instanceUrl: instance } }]; }); }
  if (provider === "servicenow") { const instance = credentials.instanceUrl; if (!instance) throw new Error("ServiceNow instance URL is missing. Reconnect required."); const me = await json(`${instance.replace(/\/$/, "")}/api/now/table/sys_user?sysparm_limit=20&sysparm_fields=sys_id,name,user_name,email,active`, token); return ((me.result ?? []) as any[]).map((u) => ({ entityType: "user", entityKey: String(u.sys_id), payload: { name: u.name, userName: u.user_name, email: u.email, active: u.active } })); }
  const sites = await json("https://api.atlassian.com/oauth/token/accessible-resources", token); const site = Array.isArray(sites) ? sites.find((item: any) => item.id) : null; if (!site?.id) throw new Error("Jira returned no accessible site."); const base = `https://api.atlassian.com/ex/jira/${encodeURIComponent(credentials.cloudId ?? site.id)}`; const projects = await json(`${base}/rest/api/3/project/search?maxResults=100`, token); const projectRows = (projects.values ?? []).map((p: any) => ({ entityType: "project", entityKey: String(p.id), payload: { key: p.key, name: p.name, projectType: p.projectTypeKey, url: p.self } })); const issues = await json(`${base}/rest/api/3/search?jql=updated%20%3E%3D%20-30d%20ORDER%20BY%20updated%20DESC&maxResults=100&fields=summary,status,issuetype,project,assignee,updated,created`, token); return [...projectRows, ...(issues.issues ?? []).map((i: any) => ({ entityType: "issue", entityKey: String(i.id), payload: { key: i.key, summary: i.fields?.summary, status: i.fields?.status?.name, issueType: i.fields?.issuetype?.name, project: i.fields?.project?.key, assignee: i.fields?.assignee?.displayName ?? null, updated: i.fields?.updated, created: i.fields?.created } }))];
}

async function reconcileProviderEntities(supabaseAdmin: any, tenantId: string, provider: string, connectionId: string, currentRows: Array<{ entityType: string; entityKey: string }>, observedAt: string) {
  const { data: existing, error } = await supabaseAdmin.from("provider_sync_entities").select("entity_type,entity_key").eq("tenant_id", tenantId).eq("provider", provider).eq("connection_id", connectionId).eq("stale", false);
  if (error) throw error;
  const currentKeys = new Set(currentRows.map((row) => `${row.entityType}:${row.entityKey}`));
  const staleRows = (existing ?? []).filter((row: any) => !currentKeys.has(`${row.entity_type}:${row.entity_key}`));
  for (const row of staleRows) {
    const { error: staleError } = await supabaseAdmin.from("provider_sync_entities").update({ stale: true, observed_at: observedAt }).eq("tenant_id", tenantId).eq("provider", provider).eq("connection_id", connectionId).eq("entity_type", row.entity_type).eq("entity_key", row.entity_key);
    if (staleError) throw staleError;
  }
  return staleRows.length;
}

async function updateConnectionEvidence(supabaseAdmin: any, tenantId: string, connectionId: string, values: Record<string, unknown>) {
  const { error } = await supabaseAdmin.from("provider_connections").update({ ...values, updated_at: new Date().toISOString() }).eq("id", connectionId).eq("tenant_id", tenantId);
  if (error) throw error;
}

export const syncReportProvider = createServerFn({ method: "POST" }).middleware([requireSupabaseAuth]).inputValidator((input: { provider: Provider; connectionId?: string }) => ({ provider: input.provider, connectionId: input.connectionId ? String(input.connectionId).trim() : null })).handler(async ({ data, context }) => {
  const { tenantId, roles } = await resolveTenant(context.supabase, context.userId); if (!roles.some((r) => ["admin", "manager", "analyst"].includes(r))) throw new Error("Analyst access is required to synchronize provider report data."); const { supabaseAdmin } = await import("@/integrations/supabase/client.server"); let query = supabaseAdmin.from("provider_connections").select("id,status,encrypted_credentials").eq("tenant_id", tenantId).eq("provider", data.provider).neq("status", "disconnected"); if (data.connectionId) query = query.eq("id", data.connectionId); const { data: connections, error } = await query.order("updated_at", { ascending: false }).limit(data.connectionId ? 1 : 2); if (error) throw error; if (!connections?.length) throw new Error(`${data.provider} is not connected.`); if (!data.connectionId && connections.length > 1) throw new Error(`Multiple ${data.provider} integration instances are configured. Select a specific instance before syncing.`); const connection = connections[0]; if (!connection.encrypted_credentials) throw new Error(`${data.provider} has no stored credentials.`); const runStarted = new Date().toISOString(); const { data: run, error: runError } = await supabaseAdmin.from("provider_sync_runs").insert({ tenant_id: tenantId, provider: data.provider, connection_id: connection.id, status: "running", started_at: runStarted }).select("id").single(); if (runError || !run) throw runError ?? new Error("Could not create sync run."); await updateConnectionEvidence(supabaseAdmin, tenantId, connection.id, { last_sync_status: "running", last_sync_attempted_at: runStarted, sync_error: null });
  try { if (data.provider === "github") { const result = await syncGitHub(tenantId, connection.id, "all", run.id); await updateConnectionEvidence(supabaseAdmin, tenantId, connection.id, { status: "connected", health_status: "healthy", health_checked_at: result.lastSuccessfulAt, health_error: null, last_sync_at: result.lastSuccessfulAt, last_sync_status: "success", last_sync_successful_at: result.lastSuccessfulAt, sync_record_count: result.recordsUpserted, sync_error: null, last_error: null, connected_at: connection.status === "connected" ? undefined : result.lastSuccessfulAt }); clearEvidenceCache(); return { ok: true as const, provider: data.provider, connectionId: connection.id, records: result.recordsUpserted, finishedAt: result.lastSuccessfulAt }; } const credentials = decryptCredentials<Credentials>(connection.encrypted_credentials); const rows = await fetchProvider(data.provider, credentials, connection.id, tenantId); const observedAt = new Date().toISOString(); const staleCount = await reconcileProviderEntities(supabaseAdmin, tenantId, data.provider, connection.id, rows, observedAt); for (const row of rows) { const { error } = await supabaseAdmin.from("provider_sync_entities").upsert({ tenant_id: tenantId, provider: data.provider, connection_id: connection.id, entity_type: row.entityType, entity_key: row.entityKey, payload: row.payload, observed_at: observedAt, sync_run_id: run.id, stale: false }, { onConflict: "tenant_id,provider,connection_id,entity_type,entity_key" }); if (error) throw error; } const finished = new Date().toISOString(); await supabaseAdmin.from("provider_sync_runs").update({ status: "success", finished_at: finished, records_seen: rows.length, records_upserted: rows.length, records_staled: staleCount }).eq("id", run.id).eq("tenant_id", tenantId); await updateConnectionEvidence(supabaseAdmin, tenantId, connection.id, { status: "connected", health_status: "healthy", health_checked_at: finished, health_error: null, last_sync_at: finished, last_sync_status: "success", last_sync_successful_at: finished, sync_record_count: rows.length, sync_error: null, last_error: null, connected_at: connection.status === "connected" ? undefined : finished }); clearEvidenceCache(); return { ok: true as const, provider: data.provider, connectionId: connection.id, records: rows.length, stale: staleCount, finishedAt: finished }; } catch (error) { const message = error instanceof Error ? error.message : String(error); await supabaseAdmin.from("provider_sync_runs").update({ status: "failed", finished_at: new Date().toISOString(), error_message: message.slice(0, 2000) }).eq("id", run.id).eq("tenant_id", tenantId); await updateConnectionEvidence(supabaseAdmin, tenantId, connection.id, { status: "failed", health_status: "unhealthy", health_checked_at: new Date().toISOString(), health_error: message.slice(0, 2000), last_sync_status: "failed", sync_error: message.slice(0, 2000), last_error: message.slice(0, 2000) }); await recordOperationalIssueSafely(supabaseAdmin as any, { tenantId, source: "sync", severity: "high", title: `${data.provider} provider sync failed`, detail: `Provider synchronization failed for connection ${connection.id}. ${message}`, relatedId: run.id }); throw new Error(message); }
});

export async function loadProviderReportData(supabase: any, userId: string, departmentKey?: string | null) { const { tenantId } = await resolveTenant(supabase, userId); const department = await resolveDepartmentContext(supabase, userId, departmentKey); const scope = `${userId}:${departmentKey ?? "default"}`; return withEvidenceCache(tenantId, "provider-report", scope, async () => { const [{ data: connections }, allowedProviders] = await Promise.all([supabase.from("provider_connections").select("id,provider,status,display_name,last_sync_at").eq("tenant_id", tenantId), getDepartmentProviders(supabase, department)]); const connected = (connections ?? []).filter((c: any) => c.status === "connected"); const scoped = allowedProviders === null ? connected : connected.filter((c: any) => allowedProviders.includes(c.provider)); const providerNames = [...new Set(scoped.map((c: any) => c.provider))]; const { data: entityData } = providerNames.length ? await supabase.from("provider_sync_entities").select("provider,connection_id,entity_type,entity_key,payload,observed_at").eq("tenant_id", tenantId).eq("stale", false).in("provider", providerNames) : { data: [] }; const { data: runData } = providerNames.length ? await supabase.from("provider_sync_runs").select("provider,connection_id,status,started_at,finished_at,records_seen,error_message").eq("tenant_id", tenantId).in("provider", providerNames).order("started_at", { ascending: false }).limit(100) : { data: [] }; return { connectedProviders: scoped, entities: entityData ?? [], runs: runData ?? [], department: { key: department.departmentKey, name: department.departmentName, unrestricted: department.unrestricted } }; }); }

export interface CorrelatedSignal { title: string; detail: string; providers: string[]; timestamp: string; evidence: Array<{ provider: string; entityType: string; entityKey: string; observedAt: string }>; }
export function deriveCorrelatedSignals(entities: any[]): CorrelatedSignal[] { const github = entities.filter((e) => e.provider === "github" && e.entity_type === "repository" && e.payload?.pushedAt); const jira = entities.filter((e) => e.provider === "jira" && e.entity_type === "issue" && (e.payload?.updated || e.payload?.created)); const signals: CorrelatedSignal[] = []; for (const repo of github) for (const issue of jira) { const repoAt = new Date(repo.payload.pushedAt).getTime(); const issueAt = new Date(issue.payload.updated ?? issue.payload.created).getTime(); if (!Number.isFinite(repoAt) || !Number.isFinite(issueAt) || Math.abs(repoAt - issueAt) > 24 * 60 * 60 * 1000) continue; signals.push({ title: `GitHub activity aligns temporally with Jira issue ${issue.payload.key}`, detail: `${repo.payload.name} was pushed near the time Jira issue ${issue.payload.key} was updated. This is a temporal correlation only; CenOps does not infer causation.`, providers: ["GitHub", "Jira"], timestamp: new Date(Math.max(repoAt, issueAt)).toISOString(), evidence: [{ provider: "GitHub", entityType: repo.entity_type, entityKey: repo.entity_key, observedAt: repo.observed_at }, { provider: "Jira", entityType: issue.entity_type, entityKey: issue.entity_key, observedAt: issue.observed_at }] }); if (signals.length >= 10) return signals; } return signals; }
