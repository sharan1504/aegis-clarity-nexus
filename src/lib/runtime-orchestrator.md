# Runtime Orchestrator

The runtime orchestrator coordinates evidence collection, deterministic policy evaluation, approval, execution, and verification. Production mutation remains behind explicit trusted capability execution and approval; the LLM cannot authorize a transition.
