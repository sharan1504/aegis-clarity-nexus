import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { Check, Filter, RefreshCw, ShieldCheck, X } from "lucide-react";
import { toast } from "sonner";
import { PageHeader, SeverityBadge, StatusPill } from "@/components/layout/AppLayout";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { useRealtime } from "@/lib/realtime";
import { bulkDecideChanges } from "@/lib/change-service";
import { useTenantContext } from "@/lib/tenant";
import { useRole } from "@/lib/rbac";
import { CHANGE_STAGES, approvalProgress } from "@/lib/change-data";
import { pageHead } from "@/lib/seo";
export const Route = createFileRoute("/_app/approvals/")({ head: () => pageHead({ path: "/approvals", title: "Approval Center — Aegis AI", description: "Review and approve Aegis change proposals." }), component: ApprovalCenter });
function ApprovalCenter() { const { records, connected, lastEventAt } = useRealtime(); const { tenantId, user, environmentMode } = useTenantContext(); const { role, can } = useRole(); const navigate = useNavigate(); const [query, setQuery] = useState(""); const [stage, setStage] = useState("all"); const [selected, setSelected] = useState<string[]>([]); const [busy, setBusy] = useState(false); const isDemo = environmentMode === "demo"; const dataSourceLabel = isDemo ? "Demo" : "Live"; const teams = useMemo(() => Array.from(new Set(records.map((r) => r.ownerTeam))).sort(), [records]); const filtered = useMemo(() => records.filter((r) => (!query || `${r.id} ${r.title} ${r.agent}`.toLowerCase().includes(query.toLowerCase())) && (stage === "all" || r.stage === stage)), [records, query, stage]); const decide = async (action: "approved" | "rejected") => { if (!tenantId || !selected.length) return; setBusy(true); try { await bulkDecideChanges(records.filter((r) => selected.includes(r.id)), action, { tenantId, actor: user?.email ?? "unknown", role }); setSelected([]); toast.success(`${action === "approved" ? "Approved" : "Rejected"} ${selected.length} change${selected.length > 1 ? "s" : ""}`); } catch (e) { toast.error("Approval action failed", { description: e instanceof Error ? e.message : "Try again." }); } finally { setBusy(false); } }; const toggle = (id: string) => setSelected((s) => s.includes(id) ? s.filter((x) => x !== id) : [...s, id]); const sourceDescription = isDemo ? "Demo change records. Approval interactions are simulated and never mutate a provider." : "Live change records only. Approval records intent; execution requires a write-capable connected integration."; return <div><PageHeader title="Approval Center" description={sourceDescription} actions={<Badge variant="outline" className="gap-1.5"><ShieldCheck className="h-3 w-3" /> {role}</Badge>} /><Card className="mb-4"><CardContent className="flex flex-wrap items-center gap-2 py-3"><Filter className="h-4 w-4 text-muted-foreground" /><Input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search change ID, title or agent…" className="w-64" /><Select value={stage} onValueChange={setStage}><SelectTrigger className="w-48"><SelectValue placeholder="Stage" /></SelectTrigger><SelectContent><SelectItem value="all">All stages</SelectItem>{CHANGE_STAGES.map((s) => <SelectItem key={s} value={s}>{s}</SelectItem>)}</SelectContent></Select><div className="ml-auto flex items-center gap-3 text-xs text-muted-foreground"><span className={`h-1.5 w-1.5 rounded-full ${isDemo ? "bg-warning" : "bg-success animate-pulse"}`} />● {dataSourceLabel} · {filtered.length} records{lastEventAt ? ` · ${new Date(lastEventAt).toLocaleTimeString()}` : ""}</div></CardContent></Card>{selected.length > 0 && <Card className="mb-3 border-primary/40"><CardContent className="flex items-center gap-2 py-3"><span className="text-sm font-medium">{selected.length} selected</span><div className="ml-auto flex gap-2"><Button variant="outline" size="sm" onClick={() => setSelected([])}>Clear</Button><Button variant="destructive" size="sm" disabled={!can("approvals.reject") || busy} onClick={() => void decide("rejected")}><X className="mr-1.5 h-4 w-4" /> Reject</Button><Button size="sm" disabled={!can("approvals.approve") || busy} onClick={() => void decide("approved")}><Check className="mr-1.5 h-4 w-4" /> Approve</Button></div></CardContent></Card>}<Card><CardContent className="p-0 overflow-x-auto"><Table><TableHeader><TableRow><TableHead className="w-10" /><TableHead>Change</TableHead><TableHead>Stage</TableHead><TableHead>Risk</TableHead><TableHead>Mode</TableHead><TableHead>Owner</TableHead><TableHead>Approvals</TableHead></TableRow></TableHeader><TableBody>{filtered.map((r) => { const p = approvalProgress(r); return <TableRow key={r.id} className="cursor-pointer hover:bg-muted/30" onClick={() => navigate({ to: "/approvals/$id", params: { id: r.id } })}><TableCell onClick={(e) => e.stopPropagation()}><input type="checkbox" checked={selected.includes(r.id)} onChange={() => toggle(r.id)} /></TableCell><TableCell><div className="flex items-center gap-2"><SeverityBadge severity={r.severity} /><div><div className="font-mono text-xs">{r.id}</div><div className="max-w-[420px] truncate text-sm font-medium">{r.title}</div><div className="text-[11px] text-muted-foreground">{r.agent}</div></div></div></TableCell><TableCell><StatusPill tone={r.stage === "Executed" ? "success" : r.stage === "Ready to Execute" ? "warning" : "info"}>{r.stage}</StatusPill></TableCell><TableCell><Badge variant="outline">{r.risk.tier} · {r.risk.score}</Badge></TableCell><TableCell>{r.executionMode}</TableCell><TableCell className="text-xs text-muted-foreground">{r.ownerTeam}</TableCell><TableCell className="text-xs">{p.done}/{p.total}</TableCell></TableRow>; })}{filtered.length === 0 && <TableRow><TableCell colSpan={7} className="py-16 text-center"><div className="font-medium">{isDemo ? "No demo approval records match your filters" : "No live approval records"}</div><div className="mt-1 text-sm text-muted-foreground">{isDemo ? "Demo changes are available for workflow evaluation and do not mutate connected providers." : "Create an approval from a live dashboard recommendation."}</div></TableCell></TableRow>}</TableBody></Table></CardContent></Card><div className="mt-4 text-xs text-muted-foreground"><RefreshCw className="mr-1 inline h-3 w-3" /> Teams available from {dataSourceLabel.toLowerCase()} records: {teams.length ? teams.join(", ") : "none yet"}.</div></div>; }
export function RiskChip({ tier, score }: { tier: string; score: number }) { return <Badge variant="outline">{tier} · {score}</Badge>; }
export function ModeChip({ mode }: { mode: string }) { return <Badge variant="secondary">{mode}</Badge>; }