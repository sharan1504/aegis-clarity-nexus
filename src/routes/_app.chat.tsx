import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useMemo, useRef, useState } from "react";
import { ArrowUp, Bot, MessageSquare, Plus, ShieldCheck, Trash2, User as UserIcon } from "lucide-react";
import { useServerFn } from "@tanstack/react-start";
import { useMutation } from "@tanstack/react-query";
import { PageHeader } from "@/components/layout/AppLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Textarea } from "@/components/ui/textarea";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import { executeEnterpriseChat, type EnterpriseChatMessage } from "@/lib/enterprise-chat.functions";
import { createChatSession, deleteChatSession, getChatSession, getMyDepartments, listChatSessions, type ChatSession, type StoredChatMessage } from "@/lib/chat-history.functions";
import { createChangeFromRecommendation } from "@/lib/change-recommendation.functions";
import { useTenantContext } from "@/lib/tenant";
import { pageHead } from "@/lib/seo";
import { toast } from "sonner";

export const Route = createFileRoute("/_app/chat")({ head: () => pageHead({ path: "/chat", title: "Aegis Enterprise AI", description: "Enterprise analysis, recommendations and evidence from connected systems." }), component: ChatPage });
type Recommendation = { title?: string; rationale?: string; impact?: string; risk?: string; nextStep?: string };
type Result = { demo?: boolean; answer?: string; analysis?: string; recommendations?: Recommendation[]; sources?: string[]; confidence?: number; actionRequired?: boolean; investigationId?: string };
type Message = EnterpriseChatMessage & { result?: Result; id?: string; createdAt?: string };
const suggestions = ["What are the biggest license optimization opportunities right now?", "Analyze the current operational risks and recommend actions.", "Give me an executive summary of the connected workspace.", "Which changes should I send to the approval center?"];
function ChatPage() {
  const { environmentMode } = useTenantContext(); const isDemo = environmentMode === "demo";
  const chat = useServerFn(executeEnterpriseChat); const createSession = useServerFn(createChatSession); const loadSessions = useServerFn(listChatSessions); const loadSession = useServerFn(getChatSession); const loadDepartments = useServerFn(getMyDepartments); const removeSession = useServerFn(deleteChatSession); const createChange = useServerFn(createChangeFromRecommendation);
  const [sessions, setSessions] = useState<ChatSession[]>([]); const [sessionId, setSessionId] = useState<string | null>(null); const [messages, setMessages] = useState<Message[]>([]); const [input, setInput] = useState(""); const [departments, setDepartments] = useState<Array<{ department_key: string; display_name: string }>>([]); const [departmentKey, setDepartmentKey] = useState<string | null>(null); const [loading, setLoading] = useState(true); const scrollRef = useRef<HTMLDivElement>(null);
  const refreshHistory = async () => { const result = await loadSessions(); setSessions(result.sessions); return result.sessions; };
  const startNewChat = async (requestedDepartment = departmentKey) => { try { const result = await createSession({ data: { departmentKey: requestedDepartment } }); setSessionId(result.session.id); setDepartmentKey(result.session.departmentKey); setMessages([]); await refreshHistory(); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not start chat."); } };
  useEffect(() => { let active = true; void (async () => { try { const [history, dept] = await Promise.all([refreshHistory(), loadDepartments()]); if (!active) return; setDepartments(dept.departments.map((d: any) => ({ department_key: d.department_key, display_name: d.display_name }))); setDepartmentKey(dept.selected); const result = history[0] ? await loadSession({ data: { sessionId: history[0].id } }) : await createSession({ data: { departmentKey: dept.selected } }); if (!active) return; setSessionId(result.session.id); setDepartmentKey(result.session.departmentKey); setMessages(result.messages.map((m: StoredChatMessage) => ({ role: m.role, content: m.content, result: m.result as Result | undefined, id: m.id, createdAt: m.createdAt }))); if (!history.length) await refreshHistory(); } catch (error) { if (active) toast.error(error instanceof Error ? error.message : "Chat history could not be loaded."); } finally { if (active) setLoading(false); } })(); return () => { active = false; }; }, []);
  useEffect(() => { scrollRef.current?.scrollTo({ top: scrollRef.current.scrollHeight, behavior: "smooth" }); }, [messages]);
  const mutation = useMutation({ mutationFn: (next: EnterpriseChatMessage[]) => chat({ data: { sessionId: sessionId!, messages: next } }), onSuccess: async (result) => { if (result.ok) { setMessages((current) => [...current, { role: "assistant", content: result.answer ?? "Analysis complete.", result: result as Result }]); await refreshHistory(); } else toast.error(result.error); } });
  const send = (text: string) => { const content = text.trim(); if (!content || mutation.isPending || !sessionId) return; const next = [...messages.map((m) => ({ role: m.role, content: m.content })), { role: "user" as const, content }]; setMessages((current) => [...current, { role: "user", content }]); setInput(""); mutation.mutate(next); };
  const openSession = async (id: string) => { try { const result = await loadSession({ data: { sessionId: id } }); setSessionId(result.session.id); setDepartmentKey(result.session.departmentKey); setMessages(result.messages.map((m: StoredChatMessage) => ({ role: m.role, content: m.content, result: m.result as Result | undefined, id: m.id, createdAt: m.createdAt }))); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not open chat."); } };
  const remove = async (id: string) => { await removeSession({ data: { sessionId: id } }); const remaining = await refreshHistory(); if (id === sessionId) { if (remaining[0]) await openSession(remaining[0].id); else await startNewChat(); } };
  const submitRecommendation = async (recommendation: Recommendation) => { try { const result = await createChange({ data: recommendation }); if (!result.ok) toast.error(result.error); else toast.success("Sent to Approval Center", { description: result.id }); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not create change."); } };
  const departmentName = useMemo(() => departments.find((d) => d.department_key === departmentKey)?.display_name ?? "Workspace-wide", [departments, departmentKey]);
  if (loading) return <div className="py-16 text-center text-sm text-muted-foreground">Loading chat…</div>;
  return <div className="flex h-[calc(100vh-7rem)] flex-col"><PageHeader title="Aegis Enterprise AI" description="Analysis + evidence + recommendations across connected enterprise systems." actions={<div className="flex items-center gap-2"><Badge variant="outline" className={isDemo ? "border-warning/50 text-warning-foreground" : "border-success/40 text-success"}><span className={`mr-1.5 inline-block h-1.5 w-1.5 rounded-full ${isDemo ? "bg-warning" : "bg-success"}`} />{isDemo ? "Demo data" : "Live data"}</Badge><Badge variant="outline"><ShieldCheck className="mr-1.5 inline h-3 w-3" />{departmentName}</Badge><Badge variant="outline">Human approval required</Badge></div>} /><div className="grid min-h-0 flex-1 gap-4 lg:grid-cols-[280px_minmax(0,1fr)]"><Card className="hidden min-h-0 overflow-hidden lg:flex lg:flex-col"><div className="flex items-center justify-between border-b p-3"><span className="text-sm font-semibold">Chat history</span><Button size="icon" variant="outline" onClick={() => void startNewChat()}><Plus className="h-4 w-4" /></Button></div>{departments.length > 1 && <div className="border-b p-3"><div className="mb-1 text-[10px] font-semibold uppercase text-muted-foreground">Data scope</div><select value={departmentKey ?? ""} onChange={(e) => void startNewChat(e.target.value)} className="h-9 w-full rounded-md border bg-background px-2 text-sm">{departments.map((d) => <option key={d.department_key} value={d.department_key}>{d.display_name}</option>)}</select></div>}<div className="min-h-0 flex-1 overflow-y-auto p-2">{sessions.map((s) => <div key={s.id} className={`mb-1 flex rounded-lg border ${s.id === sessionId ? "border-primary/40 bg-primary/5" : "border-transparent"}`}><button className="min-w-0 flex-1 px-3 py-2 text-left" onClick={() => void openSession(s.id)}><div className="truncate text-xs font-medium">{s.title}</div><div className="text-[10px] text-muted-foreground">{s.departmentName ?? "Workspace-wide"}</div></button><Button variant="ghost" size="icon" className="h-8 w-8" onClick={() => void remove(s.id)}><Trash2 className="h-3.5 w-3.5" /></Button></div>)}</div></Card><Card className="flex min-h-0 flex-col overflow-hidden"><div ref={scrollRef} className="min-h-0 flex-1 space-y-5 overflow-y-auto p-6">{!messages.length && <div className="rounded-xl border border-dashed p-5"><div className="flex items-center gap-2 text-sm font-medium"><MessageSquare className="h-4 w-4 text-primary" />Start a new {departmentName} analysis</div><div className="mt-4 flex flex-wrap gap-2">{suggestions.map((q) => <button key={q} onClick={() => send(q)} className="rounded-full border px-3 py-1.5 text-xs text-muted-foreground hover:border-primary/40">{q}</button>)}</div></div>}{messages.map((m, i) => <div key={m.id ?? `${i}-${m.createdAt ?? "message"}`} className={`flex gap-3 ${m.role === "user" ? "justify-end" : ""}`}>{m.role === "assistant" && <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-primary text-primary-foreground"><Bot className="h-4 w-4" /></div>}<div className={`max-w-[90%] rounded-2xl ${m.role === "user" ? "bg-primary px-4 py-3 text-primary-foreground" : "bg-muted/40 p-4"}`}><div className="whitespace-pre-wrap text-sm leading-relaxed">{m.content}</div>{m.result?.analysis && <div className="mt-3 border-t pt-3 text-xs text-muted-foreground">{m.result.analysis}</div>}{m.result?.recommendations?.map((r, index) => <div key={`${r.title}-${index}`} className="mt-3 rounded-lg border bg-background p-3"><div className="text-xs font-semibold">{r.title ?? "Recommendation"}</div><div className="mt-1 text-xs text-muted-foreground">{r.rationale}</div><Button size="sm" variant="outline" className="mt-2" onClick={() => void submitRecommendation(r)}>Send to Approval Center</Button></div>)}{m.result?.sources?.length ? <div className="mt-3 text-[10px] text-muted-foreground">Sources: {m.result.sources.join(" · ")}</div> : null}</div>{m.role === "user" && <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-muted"><UserIcon className="h-4 w-4" /></div>}</div>)}{mutation.isPending && <div className="text-xs text-muted-foreground">Gathering authorized {departmentName} evidence…</div>}</div>{mutation.isError && <div className="border-t p-3"><Alert variant="destructive"><AlertTitle>Enterprise AI unavailable</AlertTitle><AlertDescription>{mutation.error instanceof Error ? mutation.error.message : "Try again."}</AlertDescription></Alert></div>}<CardContent className="border-t p-3"><form onSubmit={(e) => { e.preventDefault(); send(input); }} className="flex items-end gap-2"><Textarea value={input} onChange={(e) => setInput(e.target.value)} placeholder={`Ask Aegis about ${departmentName.toLowerCase()}…`} className="min-h-[52px] resize-none" disabled={mutation.isPending || !sessionId} /><Button type="submit" size="icon" disabled={!input.trim() || mutation.isPending || !sessionId}><ArrowUp className="h-4 w-4" /></Button></form></CardContent></Card></div></div>;
}
