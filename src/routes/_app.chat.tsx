import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useRef, useState } from "react";
import { ArrowUp, Bot, CheckCircle2, ChevronRight, MessageSquare, Plus, ShieldCheck, Sparkles, Trash2, User as UserIcon, Link2 } from "lucide-react";
import { useServerFn } from "@tanstack/react-start";
import { useMutation } from "@tanstack/react-query";
import { PageHeader } from "@/components/layout/AppLayout";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { Badge } from "@/components/ui/badge";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import { executeEnterpriseChat, type EnterpriseChatMessage } from "@/lib/enterprise-chat.functions";
import { createChatSession, deleteChatSession, getChatSession, getMyDepartments, listChatSessions, type ChatSession, type StoredChatMessage } from "@/lib/chat-history.functions";
import { createChangeFromRecommendation } from "@/lib/change-recommendation.functions";
import { DEMO_DATA_ENABLED } from "@/lib/demo-data";
import { pageHead } from "@/lib/seo";
import { toast } from "sonner";
export const Route = createFileRoute("/_app/chat")({ head: () => pageHead({ path: "/chat", title: "Aegis Enterprise AI", description: "Enterprise analysis, recommendations and evidence from connected systems." }), component: ChatPage });
type Recommendation = { title?: string; rationale?: string; impact?: string; risk?: string; nextStep?: string };
type CorrelatedSignal = { title?: string; detail?: string; providers?: string[]; timestamp?: string };
type InvestigationEvidence = { investigationId: string; channel: string; tools: Array<{ provider: string; server: string; name: string; arguments: Record<string, unknown> }>; steps: string[] };
type Result = { demo?: boolean; answer?: string; analysis?: string; recommendations?: Recommendation[]; sources?: string[]; correlatedSignals?: CorrelatedSignal[]; confidence?: number; actionRequired?: boolean; fetchedAt?: string; department?: string; investigationEvidence?: InvestigationEvidence };
type Message = EnterpriseChatMessage & { result?: Result; id?: string; createdAt?: string };
const suggestions = ["What are the biggest license optimization opportunities right now?", "Analyze the current operational risks and recommend actions.", "Give me an executive summary of the connected workspace.", "Which changes should I send to the approval center?"];
function ChatPage() { const chat = useServerFn(executeEnterpriseChat); const createSession = useServerFn(createChatSession); const loadSessions = useServerFn(listChatSessions); const loadSession = useServerFn(getChatSession); const loadDepartments = useServerFn(getMyDepartments); const removeSession = useServerFn(deleteChatSession); const createChange = useServerFn(createChangeFromRecommendation); const [sessions, setSessions] = useState<ChatSession[]>([]); const [sessionId, setSessionId] = useState<string | null>(null); const [messages, setMessages] = useState<Message[]>([]); const [input, setInput] = useState(""); const [loadingHistory, setLoadingHistory] = useState(true); const [sentRecommendations, setSentRecommendations] = useState<Set<string>>(new Set()); const [departments, setDepartments] = useState<Array<{ id: string; department_key: string; display_name: string }>>([]); const [departmentKey, setDepartmentKey] = useState<string | null>(null); const [unrestricted, setUnrestricted] = useState(false); const scrollRef = useRef<HTMLDivElement>(null); const refreshHistory = async () => { const result = await loadSessions(); setSessions(result.sessions); return result.sessions; }; const startNewChat = async (requestedDepartment = departmentKey) => { try { const result = await createSession({ data: { departmentKey: requestedDepartment } }); setSessionId(result.session.id); setDepartmentKey(result.session.departmentKey); setMessages([]); await refreshHistory(); } catch (error) { toast.error("Could not start a new chat", { description: error instanceof Error ? error.message : "Try again." }); } }; useEffect(() => { let active = true; void (async () => { try { const [existing, departmentContext] = await Promise.all([refreshHistory(), loadDepartments()]); if (!active) return; setDepartments(departmentContext.departments.map((item: { id?: string; department_key: string; display_name: string }) => ({ id: item.id ?? item.department_key, department_key: item.department_key, display_name: item.display_name }))); setDepartmentKey(departmentContext.selected); setUnrestricted(departmentContext.unrestricted); const result = await createSession({ data: { departmentKey: departmentContext.selected } }); if (!active) return; setSessionId(result.session.id); setDepartmentKey(result.session.departmentKey); setMessages([]); setSessions((current) => [result.session, ...current.filter((item) => item.id !== result.session.id)]); void existing; } catch (error) { if (active) toast.error("Chat history could not be loaded", { description: error instanceof Error ? error.message : "Try again." }); } finally { if (active) setLoadingHistory(false); } })(); return () => { active = false; }; }, []); useEffect(() => { scrollRef.current?.scrollTo({ top: scrollRef.current.scrollHeight, behavior: "smooth" }); }, [messages]); const openSession = async (id: string) => { if (id === sessionId) return; try { const result = await loadSession({ data: { sessionId: id } }); setSessionId(result.session.id); setDepartmentKey(result.session.departmentKey); setMessages(result.messages.map((message: StoredChatMessage) => ({ role: message.role, content: message.content, result: message.result as Result | undefined, id: message.id, createdAt: message.createdAt }))); setSentRecommendations(new Set()); } catch (error) { toast.error("Could not open chat", { description: error instanceof Error ? error.message : "Try again." }); } }; const mutation = useMutation({ mutationFn: (next: EnterpriseChatMessage[]) => { if (!sessionId) throw new Error("Start a chat session first."); return chat({ data: { sessionId, messages: next } }); }, onSuccess: async (result) => { if (result.ok) { setMessages((current) => [...current, { role: "assistant", content: (result as { answer?: string }).answer ?? "Analysis complete.", result: result as never }]); await refreshHistory(); } } }); const send = (text: string) => { const content = text.trim(); if (!content || mutation.isPending || !sessionId) return; const next = [...messages.map(({ role, content: value }) => ({ role, content: value })), { role: "user" as const, content }]; setMessages((current) => [...current, { role: "user", content }]); setInput(""); mutation.mutate(next); }; const switchDepartment = async (nextKey: string) => { if (nextKey === departmentKey) return; await startNewChat(nextKey); }; const remove = async (id: string) => { try { await removeSession({ data: { sessionId: id } }); const remaining = await refreshHistory(); if (id === sessionId) { const replacement = remaining[0]; if (replacement) await openSession(replacement.id); else await startNewChat(); } } catch (error) { toast.error("Could not delete chat", { description: error instanceof Error ? error.message : "Try again." }); } }; const submitRecommendation = async (recommendation: Recommendation, key: string) => { if (sentRecommendations.has(key)) return; try { const result = await createChange({ data: recommendation }); if (!result.ok) { toast.error("Could not send recommendation to approvals", { description: result.error }); return; } setSentRecommendations((current) => new Set(current).add(key)); toast.success("Sent to approvals", { description: result.id, action: { label: "Open", onClick: () => { window.location.href = `/approvals/${result.id}`; } } }); } catch (error) { toast.error("Could not create change record", { description: error instanceof Error ? error.message : "Please retry." }); } }; const activeDepartmentName = departments.find((item) => item.department_key === departmentKey)?.display_name ?? (unrestricted ? "Workspace-wide" : "Department"); const latestAssistantResult = [...messages].reverse().find((message) => message.role === "assistant" && message.result)?.result; const isDemoData = latestAssistantResult?.demo ?? DEMO_DATA_ENABLED; return <div className="flex h-[calc(100vh-7rem)] flex-col"><PageHeader title="Aegis Enterprise AI" description="Analysis + evidence + recommendations across connected enterprise systems." actions={<div className="flex items-center gap-2"><Badge variant="outline" className={`gap-1.5 ${isDemoData ? "border-primary/40 text-primary" : "border-success/40 text-success"}`}><span className={`h-1.5 w-1.5 rounded-full ${isDemoData ? "bg-primary" : "bg-success"}`} /> {isDemoData ? "Demo data" : "Live data"}</Badge><Badge variant="outline" className="gap-1.5"><ShieldCheck className="h-3 w-3" /> {activeDepartmentName}</Badge><Badge variant="outline">Human approval required</Badge></div>} /><div className="grid min-h-0 flex-1 gap-4 lg:grid-cols-[280px_minmax(0,1fr)]"><Card className="hidden min-h-0 overflow-hidden lg:flex lg:flex-col"><div className="flex items-center justify-between border-b p-3"><div className="text-sm font-semibold">Chat history</div><Button size="icon" variant="outline" onClick={() => void startNewChat()} title="New chat"><Plus className="h-4 w-4" /></Button></div>{departments.length > 1 && <div className="border-b p-3"><div className="mb-1 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Data scope</div><select aria-label="Department data scope" value={departmentKey ?? ""} onChange={(event) => void switchDepartment(event.target.value)} className="h-9 w-full rounded-md border border-input bg-background px-3 text-sm">{departments.map((department) => <option key={department.department_key} value={department.department_key}>{department.display_name}</option>)}</select><p className="mt-1 text-[10px] text-muted-foreground">AI evidence is isolated to this department.</p></div>}<div className="border-b p-2"><Button variant="outline" className="w-full justify-start" onClick={() => void startNewChat()}><MessageSquare className="mr-2 h-4 w-4" /> New chat</Button></div><div className="min-h-0 flex-1 overflow-y-auto p-2">{loadingHistory ? <div className="p-4 text-xs text-muted-foreground">Loading history…</div> : sessions.length ? sessions.map((item) => <div key={item.id} className={`group mb-1 flex items-center rounded-lg border ${item.id === sessionId ? "border-primary/40 bg-primary/5" : "border-transparent hover:bg-muted/40"}`}><button className="min-w-0 flex-1 px-3 py-2 text-left" onClick={() => void openSession(item.id)}><div className="truncate text-xs font-medium">{item.title}</div><div className="mt-0.5 flex items-center gap-1 text-[10px] text-muted-foreground"><span>{item.departmentName ?? "Workspace-wide"}</span><span>·</span><span>{new Date(item.updatedAt).toLocaleString()}</span></div></button><Button variant="ghost" size="icon" className="mr-1 h-7 w-7 opacity-60 hover:opacity-100" onClick={() => void remove(item.id)} title="Delete chat"><Trash2 className="h-3.5 w-3.5" /></Button></div>) : <div className="p-4 text-xs text-muted-foreground">No saved chats yet.</div>}</div><div className="border-t p-3 text-[10px] text-muted-foreground">No application-level history limit. Chats remain available until you delete them. Each saved session keeps its own department boundary.</div></Card><Card className="flex min-h-0 flex-col overflow-hidden"><div ref={scrollRef} className="min-h-0 flex-1 space-y-5 overflow-y-auto p-6">{!messages.length && <div className="rounded-xl border border-dashed p-5"><div className="flex items-center gap-2 text-sm font-medium"><Sparkles className="h-4 w-4 text-primary" /> Start a new {activeDepartmentName} analysis</div><p className="mt-1 text-xs text-muted-foreground">This session is isolated to {activeDepartmentName}. Aegis will not use evidence from other departments.</p><div className="mt-4 flex flex-wrap gap-2">{suggestions.map((q) => <button key={q} onClick={() => send(q)} className="rounded-full border bg-background px-3 py-1.5 text-xs text-muted-foreground hover:border-primary/40 hover:text-foreground">{q}</button>)}</div></div>}{messages.map((m, i) => <div key={m.id ?? `${m.createdAt ?? "message"}-${i}`} className={`flex gap-3 ${m.role === "user" ? "justify-end" : ""}`}>{m.role === "assistant" ? <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-primary text-primary-foreground"><Bot className="h-4 w-4" /></div> : null}<div className={`max-w-[90%] rounded-2xl ${m.role === "user" ? "bg-primary text-primary-foreground px-4 py-3" : "bg-muted/40 p-4"}`}><div className="whitespace-pre-wrap text-sm leading-relaxed">{m.content}</div>{m.result && <AnalysisPanel result={m.result} sentRecommendations={sentRecommendations} onCreateChange={submitRecommendation} />}</div>{m.role === "user" ? <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-muted"><UserIcon className="h-4 w-4" /></div> : null}</div>)}{mutation.isPending && <div className="flex gap-3"><div className="flex h-8 w-8 items-center justify-center rounded-lg bg-primary text-primary-foreground"><Bot className="h-4 w-4" /></div><div className="rounded-2xl bg-muted/40 px-4 py-3 text-xs text-muted-foreground">Gathering authorized {activeDepartmentName} evidence and analyzing risk…</div></div>}</div>{mutation.isError && <div className="border-t p-3"><Alert variant="destructive"><AlertTitle>Enterprise AI unavailable</AlertTitle><AlertDescription>{mutation.error instanceof Error ? mutation.error.message : "Try again."}</AlertDescription></Alert></div>}<CardContent className="border-t p-3"><form onSubmit={(e) => { e.preventDefault(); send(input); }} className="flex items-end gap-2"><Textarea value={input} onChange={(e) => setInput(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter" && !e.shiftKey) { e.preventDefault(); send(input); } }} placeholder={`Ask Aegis about ${activeDepartmentName.toLowerCase()}…`} className="min-h-[52px] resize-none" rows={1} disabled={mutation.isPending || !sessionId} /><Button type="submit" size="icon" disabled={!input.trim() || mutation.isPending || !sessionId}><ArrowUp className="h-4 w-4" /></Button></form><div className="mt-2 flex items-center gap-1.5 text-[10px] text-muted-foreground"><ShieldCheck className="h-3 w-3" /> Department-scoped authorized data · no provider mutations from chat</div></CardContent></Card></div></div>; }
function AnalysisPanel({ result, sentRecommendations, onCreateChange }: { result: Result; sentRecommendations: Set<string>; onCreateChange: (recommendation: Recommendation, key: string) => void }) { const correlations = result.correlatedSignals?.filter((signal) => signal.title && signal.detail && signal.providers?.length) ?? []; return <div className="mt-4 space-y-3 border-t pt-3">{result.investigationEvidence && <div className="rounded-lg border border-primary/20 bg-primary/5 p-3"><div className="mb-2 flex items-center justify-between"><div className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Investigation evidence</div><Badge variant="outline">{result.investigationEvidence.channel}</Badge></div><div className="mb-3 text-[11px] text-muted-foreground">Investigation <span className="font-mono">{result.investigationEvidence.investigationId}</span> · Aegis records the exact server-side tools used, their arguments, evidence, findings and customer response.</div><div className="space-y-2">{result.investigationEvidence.tools.map((tool) => <div key={`${tool.server}-${tool.name}`} className="rounded-md border bg-background p-2"><div className="flex items-center gap-2"><Badge variant="secondary">{tool.provider}</Badge><span className="font-mono text-xs">{tool.name}</span></div><div className="mt-1 text-[10px] text-muted-foreground">Server: {tool.server}</div><pre className="mt-2 overflow-x-auto rounded bg-muted/40 p-2 text-[10px]">{JSON.stringify(tool.arguments, null, 2)}</pre></div>)}</div><div className="mt-3"><div className="mb-1 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Investigation steps</div><ol className="space-y-1 text-xs text-muted-foreground">{result.investigationEvidence.steps.map((step, index) => <li key={`${step}-${index}`}><span className="mr-2 font-mono">{index + 1}.</span>{step}</li>)}</ol></div></div>}<Section title="Analysis">{result.analysis ?? "No additional analysis was returned."}</Section>{correlations.length ? <div><div className="mb-2 flex items-center gap-1.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground"><Link2 className="h-3 w-3" /> Correlated signals</div><div className="space-y-2">{correlations.map((signal, i) => <div key={`${signal.title}-${i}`} className="rounded-lg border bg-background p-3"><div className="text-sm font-medium">{signal.title}</div><div className="mt-1 text-xs text-muted-foreground">{signal.detail}</div><div className="mt-2 flex flex-wrap gap-1">{signal.providers!.map((provider) => <Badge key={provider} variant="outline">{provider}</Badge>)}{signal.timestamp && <Badge variant="secondary">{new Date(signal.timestamp).toLocaleString()}</Badge>}</div></div>)}</div></div> : null}{result.recommendations?.length ? <div><div className="mb-2 flex items-center gap-1.5 text-xs font-semibold uppercase tracking-wider text-muted-foreground"><Sparkles className="h-3 w-3" /> Recommendations</div><div className="space-y-2">{result.recommendations.map((r, i) => { const key = `${r.title ?? "recommendation"}-${i}`; const sent = sentRecommendations.has(key); return <div key={key} className="rounded-lg border bg-background p-3"><div className="flex items-start gap-2"><ChevronRight className="mt-0.5 h-4 w-4 text-primary" /><div className="min-w-0 flex-1"><div className="text-sm font-medium">{r.title}</div><div className="mt-1 text-xs text-muted-foreground">{r.rationale}</div><div className="mt-2 flex flex-wrap gap-2 text-[11px]"><Badge variant="outline">Risk: {r.risk ?? "not classified"}</Badge><Badge variant="outline">Impact: {r.impact ?? "not quantified"}</Badge></div><div className="mt-2 text-xs"><span className="font-medium">Next step:</span> {r.nextStep ?? "Not specified"}</div><Button size="sm" variant={sent ? "secondary" : "outline"} className="mt-3" disabled={sent} onClick={() => onCreateChange(r, key)}>{sent ? "✓ Sent to approvals" : "Create change record"}</Button></div></div></div>; })}</div></div> : null}<div className="grid gap-2 sm:grid-cols-2"><Section title="Evidence sources">{result.sources?.length ? result.sources.join("\n") : "Connected authorized telemetry"}</Section><Section title="Confidence"><div className="flex items-center gap-2"><CheckCircle2 className="h-4 w-4 text-success" /> {result.confidence ?? 0}%</div></Section></div></div>; }
function Section({ title, children }: { title: string; children: React.ReactNode }) { return <div><div className="mb-1 text-xs font-semibold uppercase tracking-wider text-muted-foreground">{title}</div><div className="whitespace-pre-wrap text-xs leading-relaxed text-foreground/80">{children}</div></div>; }